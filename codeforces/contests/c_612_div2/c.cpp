#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  vector<int> nums();
  vector<bool> miss();
  for (int i = 0; i < n; i++) {
    int temp;
    cin >> temp;
    if (temp != 0)
      nums.append(temp);
    else
      have.append(i);
  }
  
  return 0;
}
