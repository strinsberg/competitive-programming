#include <bits/stdc++.h>
using namespace std;

// For each triple of lines. Find the 3 intersection points and use them
// to calculate the length of the sides. Add these together and get the
// perimeter. Keep track of the max value. If the perimeter is -1 at the end
// there is no triangle possible. Probably need howards line intersection
// code

int main() {

  return 0;
}
